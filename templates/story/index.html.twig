{% extends 'base.html.twig' %}

{% block title %}Testez votre solution PHP !{% endblock %}

{% block body %}
	<div class="container">
		<div class="row">
			<div class="col s12 m12 l12">
				<h4>Historiques des liens https de votre projet Git :</h4>
                    <table class="table table-dark table-striped">
                            <thead>
                                <tr>
                                    <th scope="col">#</th>
                                    <th scope="col">Liens</th>
                                    <th scope="col">Date</th>
                                    <th scope="col">Version</th>
                                </tr>
                            </thead>
                            <tbody>
                        {% for story in stories %}
                                <tr> 
                                    <td>{{ loop.index }}</td>
                                    <td>{{ story.appGitLink }}</td>
                                    <td>{{ story.appTestDate | date('d-m-y h:s:m') }}</td>
                                    <td>{{ story.appPhpVer }}</td>
                                </tr>
                        {% endfor %}
                            </tbody>
                    </table>
			</div>
		</div>
        <a href="{{ ('/add') }}" class="btn btn-primary">Retour à la page d'accueil</a>
	</div>
{% endblock %}
